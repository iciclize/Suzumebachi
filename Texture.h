#ifndef YJSNPI_TEXTURE
#define YJSNPI_TEXTURE

#include <stdio.h>
#include <GL/glut.h>

GLuint loadTexture(char *filename, unsigned char aR, unsigned char aG, unsigned char aB);

#endif /* YJSNPI_TEXTURE */
